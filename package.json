{
  "name": "smart-timer-desktop",
  "version": "1.0.0",
  "description": "Desktop windowed timer app built with Electron.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package:win": "electron-packager . SmartTimer --platform=win32 --arch=x64 --overwrite --out=dist"
  },
  "devDependencies": {
    "electron": "^28.2.0",
    "electron-packager": "^17.1.2"
  }
}
